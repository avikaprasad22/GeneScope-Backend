services:
        web:
                # image: genescope
                build: .
                env_file:
                        - .env # This file is optional; defaults will be used if it does not exist
                ports:
                        - "5329:5329"
                volumes:
                        - ./instance:/instance
                restart: unless-stopped